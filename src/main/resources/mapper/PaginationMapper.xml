<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jolipjo.lovebridge.domain.paginaition.dao.PaginationMapper">
    <!--    <select id="selectItemsWithPagination" parameterType="map" resultType="AnotherCategoryItem">-->
    <!--    SELECT-->
    <!--           *-->
    <!--      FROM-->
    <!--           another_category_table-->
    <!--    ORDER BY-->
    <!--           item_id-->
    <!--    OFFSET-->
    <!--            #{offset} ROWS FETCH NEXT #{size} ROWS ONLY-->
    <!--    </select>-->

    <select id="findAllByPage" resultType="com.jolipjo.lovebridge.domain.paginaition.dto.PaginationDTO">
        SELECT
        *
        FROM
        items
        ORDER BY id
        LIMIT #{size} OFFSET #{offset}
    </select>
</mapper>
